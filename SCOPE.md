# SCOPE — what is / is not proved for the prize (JSP-000314)

## Headline target

- Required theorem: `bad_interval_count_asymptotic`
- Status at scaffold: declaration present with `sorry`.

## What the headline covers

Ta26c: if B(x) counts n≤x lying in some bad interval (product of consecutives divisible by square of its largest prime factor), then B(x) = (1 + O((log x)^{-1+o(1)})) · #{n≤x : P(n)^2 | n}.

## Prize rules reminder

Do not open award claim issues / PR TheJustinSunPrize/awards.
